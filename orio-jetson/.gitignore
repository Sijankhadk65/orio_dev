# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Piper voice models (downloaded, ~60MB each — fetch with piper.download_voices)
voices/

# YOLO weights (downloaded by ultralytics on first run)
models/yolo/

# Knowledge-base collections (sqlite-vec dbs — local/per-deployment data,
# not source; the "orio" profile self-seeds, others come from kb_ingest.py)
kb/

# Secrets — copy .env.example to .env and fill in your own values.
.env

# Local machine settings — copy settings.example.json to settings.json and edit.
settings.json
