"""Orio — on-Jetson LLM operator layer.

The conversational brain that sits on top of the deterministic mediator / STM32
stack. For now it only converses (LLM via Ollama) and speaks (TTS); semantic
tool calls (drive_to, move_arm_to, stop, get_status) are added later.
"""
